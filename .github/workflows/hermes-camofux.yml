name: Hermes MCP + Camoufox (NPM)

on:
  workflow_dispatch:

concurrency:
  group: hermes-production
  cancel-in-progress: true

jobs:
  hermes:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Restore Hermes Cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.hermes
            ~/.npm
            ~/.camofox
          key: hermes-data-${{ github.run_id }}
          restore-keys: |
            hermes-data-

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install and Start Camoufox (Background)
        run: |
          # 1. Clone the stealth browser repo
          git clone https://github.com/jo-inc/camofox-browser
          cd camofox-browser
          
          # 2. Install dependencies
          npm install
          
          # 3. Start the server in the background
          # Default port is 9377. We use nohup to ensure it persists across steps.
          export CAMOFOX_PORT=9377
          nohup npm start > camofox.log 2>&1 &
          
          # 4. Wait for the browser to download and the health check to pass
          echo "Waiting for Camoufox to download and start (up to 5 minutes)..."
          TIMEOUT=300
          until curl -s http://localhost:9377/health; do
            if [ $TIMEOUT -le 0 ]; then 
              echo "ERROR: Camoufox failed to start within 5 minutes."
              cat camofox.log
              exit 1 
            fi
            sleep 10
            TIMEOUT=$((TIMEOUT-10))
            echo "Still waiting... ($TIMEOUT seconds remaining)"
          done
          echo "Camoufox is ONLINE at http://localhost:9377"

      - name: Install Hermes
        run: |
          # Clean up any broken repo state from cache to prevent git errors
          if [ -d "$HOME/.hermes/hermes-agent" ]; then
            cd "$HOME/.hermes/hermes-agent"
            git reset --hard HEAD && git clean -fd
            cd $GITHUB_WORKSPACE
          fi
          
          # Run official installer
          echo "y" | bash -c "$(curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh)"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Configure Hermes
        env:
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }} # Added back
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_ALLOWED_USERS: ${{ secrets.TELEGRAM_ALLOWED_USERS }} # Added back
          TELEGRAM_HOME_CHANNEL: ${{ secrets.TELEGRAM_HOME_CHANNEL }} # Added back
          BRIGHTDATA_API_TOKEN: ${{ secrets.BRIGHTDATA_API_TOKEN }}
          SCRAPERAPI_KEY: ${{ secrets.SCRAPERAPI_KEY }}
        run: |
          mkdir -p ~/.hermes
          
          # Write .env with the local Camoufox URL
          cat > ~/.hermes/.env << EOF
          NVIDIA_API_KEY=${NVIDIA_API_KEY}
          GOOGLE_API_KEY=${GOOGLE_API_KEY}
          TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
          TELEGRAM_ALLOWED_USERS=${TELEGRAM_ALLOWED_USERS}
          TELEGRAM_HOME_CHANNEL=${TELEGRAM_HOME_CHANNEL}
          CAMOFOX_URL=http://localhost:9377
          EOF

          # Write config.yaml
          cat > ~/.hermes/config.yaml << EOF
          model:
            default: nvidia/nemotron-3-super-120b-a12b
            provider: nvidia
          platforms:
            telegram:
              enabled: true
          mcp_servers:
            brightdata:
              command: "npx"
              args: ["-y", "@brightdata/mcp"]
              env:
                API_TOKEN: "${BRIGHTDATA_API_TOKEN}"
          browser:
            camofox:
              managed_persistence: true
          agent:
            max_turns: 90
            yolo: true
          EOF

      - name: Run Hermes Gateway
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          # Gateway starts here. It will use the background Camoufox for browser tools.
          hermes gateway

      - name: Save Hermes data
        if: always()
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.hermes
            ~/.camofox
          key: hermes-data-${{ github.run_id }}
