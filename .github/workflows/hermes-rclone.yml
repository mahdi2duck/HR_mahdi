name: Hermes MCP + Camoufox (NPM) + rclone

on:
  workflow_dispatch:

concurrency:
  group: hermes-production
  cancel-in-progress: true

jobs:
  hermes:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install and Configure Rclone
        env:
          R2_ACCESS_KEY: ${{ secrets.R2_ACCESS_KEY }}
          R2_SECRET_KEY: ${{ secrets.R2_SECRET_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
        run: |
          # Install rclone
          curl -fsSL https://rclone.org/install.sh | sudo bash

          # Write rclone config
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf << EOF
          [r2]
          type = s3
          provider = Cloudflare
          access_key_id = ${R2_ACCESS_KEY}
          secret_access_key = ${R2_SECRET_KEY}
          endpoint = https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com
          acl = private
          EOF

      - name: Restore Hermes State from R2
        run: |
          echo "Pulling latest Hermes state from R2..."
          rclone sync r2:hermes-state/hermes ~/.hermes \
            --create-empty-src-dirs \
            --transfers=8 \
            --log-level INFO
          echo "Restore complete."

      - name: Restore Camoufox Cache from R2
        run: |
          echo "Pulling Camoufox cache from R2..."
          rclone sync r2:hermes-state/camofox ~/.camofox \
            --create-empty-src-dirs \
            --transfers=8 \
            --log-level INFO
          echo "Camoufox cache restore complete."

      - name: Install and Start Camoufox (Background)
        run: |
          # 1. Clone the stealth browser repo
          git clone https://github.com/jo-inc/camofox-browser
          cd camofox-browser

          # 2. Install dependencies
          npm install

          # 3. Start the server in the background
          export CAMOFOX_PORT=9377
          nohup npm start > camofox.log 2>&1 &

          # 4. Wait for the browser to download and the health check to pass
          echo "Waiting for Camoufox to download and start (up to 5 minutes)..."
          TIMEOUT=300
          until curl -s http://localhost:9377/health; do
            if [ $TIMEOUT -le 0 ]; then
              echo "ERROR: Camoufox failed to start within 5 minutes."
              cat camofox.log
              exit 1
            fi
            sleep 10
            TIMEOUT=$((TIMEOUT-10))
            echo "Still waiting... ($TIMEOUT seconds remaining)"
          done
          echo "Camoufox is ONLINE at http://localhost:9377"

      - name: Install Hermes
        run: |
          # Clean up any broken repo state from cache to prevent git errors
          if [ -d "$HOME/.hermes/hermes-agent" ]; then
            cd "$HOME/.hermes/hermes-agent"
            git reset --hard HEAD && git clean -fd
            cd $GITHUB_WORKSPACE
          fi

          # Run official installer
          echo "y" | bash -c "$(curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh)"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Configure Hermes
        env:
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_ALLOWED_USERS: ${{ secrets.TELEGRAM_ALLOWED_USERS }}
          TELEGRAM_HOME_CHANNEL: ${{ secrets.TELEGRAM_HOME_CHANNEL }}
          BRIGHTDATA_API_TOKEN: ${{ secrets.BRIGHTDATA_API_TOKEN }}
          SCRAPERAPI_KEY: ${{ secrets.SCRAPERAPI_KEY }}
        run: |
          mkdir -p ~/.hermes

          # .env and config.yaml are always regenerated fresh from GitHub Secrets
          # They are excluded from R2 sync — secrets never leave the runner
          cat > ~/.hermes/.env << EOF
          NVIDIA_API_KEY=${NVIDIA_API_KEY}
          GOOGLE_API_KEY=${GOOGLE_API_KEY}
          TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
          TELEGRAM_ALLOWED_USERS=${TELEGRAM_ALLOWED_USERS}
          TELEGRAM_HOME_CHANNEL=${TELEGRAM_HOME_CHANNEL}
          CAMOFOX_URL=http://localhost:9377
          EOF

          cat > ~/.hermes/config.yaml << EOF
          model:
            default: nvidia/nemotron-3-super-120b-a12b
            provider: nvidia
          platforms:
            telegram:
              enabled: true
          mcp_servers:
            brightdata:
              command: "npx"
              args: ["-y", "@brightdata/mcp"]
              env:
                API_TOKEN: "${BRIGHTDATA_API_TOKEN}"
          browser:
            camofox:
              managed_persistence: true
          agent:
            max_turns: 90
            yolo: true
          EOF

      - name: Start Rclone Sync Watcher (Background)
        run: |
          # Exclude files that are always regenerated from GitHub Secrets
          cat > /tmp/rclone-exclude.txt << EOF
          .env
          config.yaml
          EOF

          # Sync ~/.hermes to R2 every 60 seconds in the background
          while true; do
            sleep 60
            rclone sync ~/.hermes r2:hermes-state/hermes \
              --exclude-from /tmp/rclone-exclude.txt \
              --transfers=4 \
              --checksum \
              --log-level ERROR
          done &
          echo "Rclone watcher started (PID: $!), syncing every 60s"

      - name: Run Hermes Gateway
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          hermes gateway

      - name: Final Sync to R2 on Exit
        if: always()
        run: |
          echo "Running final sync to R2..."
          rclone sync ~/.hermes r2:hermes-state/hermes \
            --exclude-from /tmp/rclone-exclude.txt \
            --transfers=8 \
            --checksum \
            --log-level INFO
          rclone sync ~/.camofox r2:hermes-state/camofox \
            --transfers=8 \
            --checksum \
            --log-level INFO
          echo "Final sync complete."